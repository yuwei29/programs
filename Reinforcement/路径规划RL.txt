路径规划
三维
以 1000*1000*1000 个格子 为例，
假设 直到到达目标点，每一步不重复，则
最多走 N steps, 此处 N = 1e9 ,最多可能情形为 N! 约等于 N^N 
所以 约为 1e9^(1e9) = 10^(9e9) 种情形

如果 每一步 最多有 10 种 选择，则为 10^N = 10^(1e9) 种 情形


所以重点是 价值的 估算 函数 

牺牲精度换取速度
用估算、近似计算 节省 时间 

